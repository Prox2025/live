name: Iniciar Live com Cache e Criação Automática do package.json

on:
  workflow_dispatch:
    inputs:
      id:
        description: 'ID do vídeo (nome do arquivo no Google Drive)'
        required: true
      stream_url:
        description: 'URL do stream (RTMP do Facebook ou outro)'
        required: true

jobs:
  run-live:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositório
        uses: actions/checkout@v4

      - name: ⚙️ Criar package.json automaticamente
        run: |
          echo '{
            "name": "live-stream",
            "version": "1.0.0",
            "dependencies": {
              "axios": "^1.4.0",
              "fs-extra": "^11.1.1",
              "puppeteer": "^21.3.8",
              "googleapis": "^133.0.0"
            }
          }' > package.json

      - name: 📦 Cache do npm
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-cache-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-cache-

      - name: 📥 Instalar dependências
        run: npm install

      - name: 🛠️ Instalar ffmpeg
        run: sudo apt update && sudo apt install -y ffmpeg

      - name: 🧾 Criar arquivo input.json
        run: |
          echo '{
            "id": "${{ github.event.inputs.id }}",
            "stream_url": "${{ github.event.inputs.stream_url }}"
          }' > input.json

      - name: ▶️ Executar live.js
        run: node live.js input.json chave.json

      - name: ✅ Upload do vídeo original (para depuração)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: video-${{ github.event.inputs.id }}
          path: ${{ github.event.inputs.id }}.mp4
